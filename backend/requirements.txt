# --- AtmosGuard MVP runtime ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
numpy==2.2.1

# --- Optional: enables the real (trained) risk model path ---
# The MVP ships an interpretable linear baseline (app/core/risk_model.py).
# Installing these lets `app/core/model_registry.py` load a trained
# XGBoost model + real SHAP explanations without any API change.
# xgboost==2.1.3
# scikit-learn==1.6.0
# shap==0.46.0

# --- Optional: real NWP/ensemble ingestion (replaces synthetic provider) ---
# xarray==2024.11.0
# netCDF4==1.7.2
# cfgrib==0.9.14.1

# --- Optional: PostgreSQL / TimescaleDB instead of local SQLite ---
# psycopg[binary]==3.2.3

# --- Dev ---
# pytest==8.3.4
# httpx==0.28.1
